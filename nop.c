#include "monty.h"

/**
* monty_nop - Does nothing.
* @stack: A pointer to the top of the stack.
* @counter: The line number in Monty file where nop was called.
*/
void monty_nop(stack_t **stack, unsigned int counter)
{
	(void)stack;
	(void)counter;
	/* No operation */
}
